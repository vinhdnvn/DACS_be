<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="">
      <div class="">
        <form
          action="/events/<%-event._id%>/edit"
          method="post"
          enctype="multipart/form-data"
        >
          <div class="mt-4">
            <input
              type="text"
              placeholder="Name of Event"
              name="name"
              required
              class="w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"
              value="<%- event.name%>"
            />
          </div>
          <div class="mt-4">
            <input
              type="text"
              placeholder="Location"
              name="locateAt"
              required
              class="w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"
              value="<%- event.locateAt%>"
            />
          </div>
          <div class="mt-4">
            <label for="eventTypes">Choose a Type of Event :</label>
            <input
              type="text"
              list="event_Type"
              id="eventTypes"
              name="eventType"
              value="<%- event.eventType%>"
            />
            <datalist id="event_Type">
              <option value="Meeting"></option>
              <option value="Aniversary"></option>
              <option value="Club Activity"></option>
              <option value="Competition"></option>
              <option value="Celebration"></option>
            </datalist>
          </div>
          <div class="mt-4">
            <input
              type="text"
              placeholder="Note"
              name="note"
              class="w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"
              value="<%- event.note%>"
            />
          </div>
          <div class="mt-4">
            <label for="updates">Choose a Date to start Event: </label>
            <input
              id="updates"
              type="date"
              placeholder="Date"
              name="update"
              required
              class="w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"
              value="<%- event.createUpdate%>"
            />
          </div>
          <div class="mt-4">
            <label for="images">Upload image file : </label>
            <input
              id="images"
              type="file"
              placeholder="Event Image"
              name="eventImage"
              required
              class="w-full px-4 py-2 mt-2 border rounded-md focus:outline-none focus:ring-1 focus:ring-blue-600"
              value="<%- event.eventImage%>"
            />
          </div>
          <div class="mt-4 flex">
            <button
              type="submit"
              class="text-white bg-blue-600 rounded-lg hover:bg-blue-900"
            >
              Cập nhật
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
