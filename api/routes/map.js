const express = require("express");
const { path } = require("../../app");
const router = express.Router();

// router.get("/map", (req, res) => {
//   res.sendFile(__dirname + "/testLayercontrol/main.html");
// });

module.exports = router;
